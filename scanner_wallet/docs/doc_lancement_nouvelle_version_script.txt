
#monitoring du nombre de nouveaux tokens, update, transactions, snapshot 
#monitoring du nombre total de tokens, nombre de tokens ruggés, morts, sans données...
python token_metrics.py --watch

#rapport qui donne une liste de tokens respectants certains critères de filtres modifiables
python -m streamlit run opportunity_trade_streamlit.py --server.port 8504

#script de test : comparaison pour un token entre les données en base et les données récupérées depuis dexscreener / pump.fun et rugcheck.xyz
python token_comparison.py 

#rapport pour monitorer le nombre de transactions, nouveaux tokens, snapshots et updates à intervalles réguliers
#comme token_metrics.py mais en streamlit
python -m streamlit run token_metrics_streamlit.py --server.port 8503

#rapport principal avec tokens overview, détail token et onglet monitoring transactions et onglet données historiques
python -m streamlit run streamlit_metric_stat.py --server.address 127.0.0.1 --server.fileWatcherType=watchdog

#rapport pour les données historiques pour chaque tokens
python -m streamlit run token_history_metrics_streamlit.py --server.port 8504

#script qui récupère les tokens depuis la table transactions et enrichissement avec dexscreener / pump.fun
#et rugcheck.xyz et alimentation de la table tokens_history à partir de la table tokens pour historisation
python get_dexscreener_info.py

#script pour détecter les transactions dans les wallets suivis, remplissage de la table transactions
python run_scanner.py

#script pour détecter les rug pulls (après coup) et mettre à jour les tokens rug pulled
python rug_pull_detector_and_update_rug_pull.py